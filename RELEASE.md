# Release

 1. Bump the version in `action.yml`, e.g. `@1.2.0`
 2. `git commit -m 'new release: 1.2.0`
 3. `git tag -a v1.2.0`
 4. `git push --tags`
 5. _wait for the build_
 6. create release for the marketplace